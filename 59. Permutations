class Solution {
    public void helper(int[] nums, List<Integer> list, boolean isValid[] ,List<List<Integer>> ans){
        if(list.size() == nums.length){
            ans.add(new ArrayList<>(list));
            return;
        }

        for(int i=0;i<nums.length;i++){
            if(isValid[i] == false){
                int x = nums[i];
                list.add(x);
                isValid[i] = true;
                helper(nums,list,isValid,ans);
                isValid[i] = false;
                list.remove(list.size()-1);
            }
           
        }
    }
    public List<List<Integer>> permute(int[] nums) {
        List<List<Integer>> ans = new ArrayList<>();
        List<Integer> list = new ArrayList<>();

        boolean isValid[] = new boolean[nums.length];
        helper(nums, list, isValid, ans);
        return ans;
    }
}








// class Solution {

//     public void helper(int[] nums, List<List<Integer>> ans, List<Integer> list, boolean isValid[] ){

//         if(nums.length == list.size()){
//             List<Integer> myList = new ArrayList<>(list);
//             ans.add(myList);
//             return;
//         }

//         for(int i=0;i<nums.length;i++){

//             if(isValid[i] == false){
//                 list.add(nums[i]);
//                 isValid[i] = true;
//                 helper(nums,ans,list,isValid);
//                 isValid[i] = false;
//                 list.remove(list.size()-1);
//             }
//         }

//     }
//     public List<List<Integer>> permute(int[] nums) {
        
//         List<List<Integer>> ans = new ArrayList<>();
//         List<Integer> list = new ArrayList<>();
//         boolean isValid[] = new boolean[nums.length];

//         helper(nums, ans,list,isValid);
//         return ans;
//     }
// }
